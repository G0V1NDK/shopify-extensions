{
  "name": "hc-shelf-label-pos",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "react-to-print": "^3.1.1"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "govind",
  "private": true,
  "workspaces": [
    "extensions/*"
  ]
}
